package com.array;
//objects of a class, the actual objects are stored in heap segment.
//object clone method overrides clone method in class Object and throws no checked exceptions.
//such as Object[], a “deep copy” is performed with the new array containing copies of the original array’s elements as opposed to references.
//multidimensional array (like Object[][]) is a “shallow copy”  but intArray[0] == cloneArray[0] return true

public class RotateArr {
	public static void code() {

	}
}
